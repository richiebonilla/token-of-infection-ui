import { IProviderOptions } from "../helpers/types";
interface ICoreOptions {
    modal?: boolean;
    network?: string;
    lightboxOpacity?: number;
    providerOptions?: IProviderOptions;
}
declare class Core {
    private show;
    private eventManager;
    private injectedProvider;
    private network;
    private lightboxOpacity;
    private providerOptions;
    private providers;
    constructor(opts?: ICoreOptions);
    on(event: string, callback: (result: any) => void): void;
    connectTo: (type: string, connector: (opts: any) => Promise<any>) => Promise<void>;
    toggleModal: () => Promise<void>;
    getProviders: () => {
        name: string | null;
        onClick: () => Promise<void>;
    }[];
    private onError;
    private onConnect;
    private onClose;
    private updateState;
    private resetState;
    renderModal(): void;
}
export default Core;
//# sourceMappingURL=index.d.ts.map